PyQt5
hid
NumPy
# >=12.3.0 for CVE-2026-54058 (out-of-bounds read decoding a McIdas AREA
# image opened by filename). im_converter.open() calls Image.open(filename)
# on a user-chosen overlay image, which is that exact vector. An unpinned
# `Pillow` is satisfied by an already-installed old one, so pip would not
# upgrade it -- the floor is what forces it.
Pillow>=12.3.0
PyWinCtl
pywin32; sys_platform == "win32"
pynput
PyYAML
platformdirs
requests
freetype-py
uharfbuzz
fonttools
pyserial
packaging
jeepney; sys_platform == "linux"
# NOTE: the program icon does NOT need cairosvg. It is rasterised through
# freetype-py (above) by services/svg_raster, because cairocffi publishes no
# Windows wheel and bundles no cairo, so `import cairosvg` raises on a stock
# Windows Python. cairosvg is used as a fallback if it happens to be installed.
